# PostgreSQL (local o Railway). Ejemplo Railway:
# DATABASE_URL="postgresql://usuario:contraseña@host.railway.app:puerto/railway"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# NextAuth (obligatorio en producción)
AUTH_SECRET="generar-con-npx-auth-secret"
AUTH_URL="http://localhost:3000"

# OAuth (opcionales; si no usas un proveedor, no lo definas)
# AUTH_GOOGLE_ID="..."
# AUTH_GOOGLE_SECRET="..."
# AUTH_GITHUB_ID="..."
# AUTH_GITHUB_SECRET="..."

# Cron de scraping (Vercel Cron llama a GET /api/cron/scrape-jobs con este secreto en Authorization: Bearer <CRON_SECRET>)
CRON_SECRET="generar-un-secreto-aleatorio"

# SerpApi (opcional). Para vacantes de LinkedIn: key global del servidor O que cada usuario guarde la suya en Configuración (cifrada en BD).
# SERPAPI_API_KEY="tu-api-key"

# Activar/desactivar fuentes de scraping (por defecto todas activas). Poner "false" o "0" para desactivar.
# ENABLE_REMOTIVE="true"
# ENABLE_REMOTEOK="true"
# ENABLE_LINKEDIN="true"
